//At the moment these defines assume linux is the target platform.
#ifdef ZEDBOARD
#define BASE_GPIO (906)
#define N_GPIO (118)
#endif
#ifdef PI3_B_PLUS
#define BASE_GPIO (0)
#define N_GPIO (27)
#endif
